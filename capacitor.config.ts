import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.einbuergerungstest.app',
  appName: 'Einbürgerungstest Quiz',
  webDir: 'dist/public'
};

export default config;
