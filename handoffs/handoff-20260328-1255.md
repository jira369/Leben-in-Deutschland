# Session Handoff

**From:** Claude Opus 4.6 session (branch merge + release build)
**To:** Next session
**Date:** 2026-03-28 12:55

---

## Status Summary
goofy-haibt wurde erfolgreich in main gemergt, alle 460 BAMF-verifizierten Fragen konvertiert (0-based indexing), der Lukas-Bug gefixt, und eine signierte AAB-Datei (v3.2.1, versionCode 321) für den Google Play Store erstellt. Main ist gepusht und up-to-date. Die AAB-Datei liegt bereit unter `einbuergerungstest-v3.2.1.aab` (3.1 MB).

## What Was Done
- Merged `claude/goofy-haibt` into `main` — Capacitor local storage backend, UI polish, native platform support, removed Replit dependencies
- Converted 460 verified BAMF questions from 1-based to 0-based `correctAnswer` indexing
- Renamed "NRW" category to "Nordrhein-Westfalen" in question data (10 questions)
- Fixed Android package name from `de.einbuergerungstest.app` to `de.lebenindeutschland.quiz` (build.gradle, strings.xml, MainActivity.java)
- Updated `capacitor.config.ts` with correct appId + plugin config (SplashScreen, StatusBar, Keyboard)
- Bumped version to 3.2.1 (versionCode 321)
- Fixed Lukas bug: `queryClient.ts` now falls back to local embedded question data when server is unreachable or returns empty array
- Fixed `nodemailer.createTransporter` typo -> `createTransport` in `server/routes.ts`
- Fixed TypeScript type annotation for quiz stats in `results.tsx`
- Added `dotenv` support and localhost binding to `server/index.ts`
- Built signed AAB with JDK 21 (Temurin) + Gradle 8.14.3
- Pushed everything to `origin/main`

## What's In Progress
- **Lukas Alpha Test feedback** — Bug fix is deployed in the new AAB. Lukas needs to test the new build to confirm fix.

## Decisions Made
| Decision | Rationale | Reversible? |
|----------|-----------|-------------|
| App ID: `de.lebenindeutschland.quiz` | User confirmed, consistent with Android project already setup | No (Play Store identity) |
| Version 3.2.1 / versionCode 321 | User confirmed bump for new Play Store upload | Yes |
| goofy-haibt code as base, nostalgic-pike data | goofy-haibt has better mobile infra, nostalgic-pike has verified questions | No |
| Local data fallback in queryClient | Fixes both native-offline and empty-DB scenarios | Yes |
| Keep Neon serverless DB driver | Production DB is Neon; postgres-js only useful for local dev | Yes |
| JDK 21 for Gradle builds | Java 25 (system default) is too new for Gradle 8.14.3; JDK 17 too old for Capacitor Android | No |

## Files Changed
| File | Change |
|------|--------|
| `android/app/build.gradle` | Package name `de.lebenindeutschland.quiz`, version 3.2.1/321 |
| `android/app/src/main/java/de/lebenindeutschland/quiz/MainActivity.java` | Moved from `einbuergerungstest/app/` path, updated package declaration |
| `android/app/src/main/res/values/strings.xml` | Updated package_name and custom_url_scheme |
| `android/app/capacitor.build.gradle` | Updated by `cap sync` |
| `android/capacitor.settings.gradle` | Updated by `cap sync` with 6 Capacitor plugins |
| `capacitor.config.ts` | appId=`de.lebenindeutschland.quiz`, plugin config for SplashScreen/StatusBar/Keyboard |
| `questions-data.json` | 460 BAMF questions, correctAnswer converted 1-based->0-based, NRW->Nordrhein-Westfalen |
| `client/src/data/questions-data.json` | Same conversion as root questions-data.json (for local storage backend) |
| `client/src/lib/queryClient.ts` | Added fallback to local data when server unreachable or returns empty questions |
| `client/src/pages/results.tsx` | Added TypeScript type for stats query |
| `package.json` | Version 3.2.1, added dotenv dependency |
| `package-lock.json` | Lockfile update |
| `server/index.ts` | Added `import 'dotenv/config'`, changed host to 127.0.0.1 |
| `server/routes.ts` | Fixed `createTransporter` -> `createTransport`, fixed error type narrowing |
| `client/public/sw.js` | Cache version updated to v3.2.1-20260328 |
| `.gitignore` | Android build artifacts ignored, but android/ project committed |
| `vite.config.ts` | Clean version without Replit plugins |

## Next Steps (Priority Order)
1. **Upload AAB to Google Play Console** — File: `einbuergerungstest-v3.2.1.aab` (3.1 MB), ready for upload
2. **Send new build to Lukas** for Alpha testing — verify the 0-questions bug is fixed
3. **Seed database with questions** — The web version needs 460 questions loaded into PostgreSQL for `/api/questions` to work (the local fallback is a workaround, not a permanent fix)
4. **Clean up old branches** — `claude/nostalgic-pike`, `claude/goofy-haibt`, `claude/wonderful-driscoll` can be deleted after confirming main is stable
5. **Consider removing nostalgic-pike worktree** — It served its purpose

## Blocked Items
- **Web version question loading** — Depends on database seeding. No seed script exists yet. Workaround: local fallback in queryClient.ts covers this for now.

## Open Questions
- Should the old branches (goofy-haibt, nostalgic-pike, wonderful-driscoll) be deleted?
- Should a database seeding script be created for the web deployment?
- Does the Play Store listing need updates (screenshots, description) for v3.2.1?

## Risks
- **Java version on CI/CD** — If automated builds are ever set up, they need JDK 21 (not 17, not 25). JDK 21 was downloaded to `/Users/dacvu/java/jdk-21.0.10+7/`
- **Keystore not in git** — `einbuergerungstest-release.jks` is gitignored. If lost, a new signing key is needed and Play Store won't accept updates. Back it up!
- **correctAnswer indexing** — The entire app now uses 0-based indexing. Any future question imports MUST use 0-based correctAnswer values (0-3). The verified BAMF source uses 1-based (1-4).
- **Pre-existing TypeScript errors** — `server/storage.ts` and `server/vite.ts` have Drizzle ORM type issues that predate this session. The build succeeds (esbuild doesn't type-check), but `npm run check` will show errors.

## Key Files to Read
- `client/src/lib/queryClient.ts` — The data fetching layer with the local fallback logic
- `client/src/lib/local-storage-backend.ts` — Full offline storage implementation for native app
- `client/src/lib/local-api-router.ts` — Routes API calls to local storage on native
- `android/app/build.gradle` — Signing config, version, package name
- `questions-data.json` — The 460 verified BAMF questions (0-based indexing)

## Gotchas
- **JDK 21 required for Android builds** — System Java is 25 (too new). Use: `JAVA_HOME=/Users/dacvu/java/jdk-21.0.10+7/Contents/Home`
- **android/local.properties not in git** — Must contain `sdk.dir=/Users/dacvu/android-sdk` (gitignored)
- **Keystore password hardcoded** in `android/app/build.gradle` — `einbuergerungstest123` (both store and key password)
- **`npx cap sync android` must run before AAB build** — Copies web assets to Android project
- **questions-data.json exists in TWO locations** — Root (for DB import) and `client/src/data/` (for Capacitor). Keep them in sync!
- **nostalgic-pike worktree** is still at `/Users/dacvu/Leben-in-Deutschland/.claude/worktrees/nostalgic-pike` with stashed changes

---
Handoff complete. Read this before starting work.
